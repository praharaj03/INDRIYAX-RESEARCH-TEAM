// TODO (Backend Dev): Implement Payment Mongoose model
//
// Fields: clerkUserId, registrationId, eventId, eventTitle, amount,
//         currency (INR), status (created|paid|failed|refunded),
//         provider (razorpay|stripe|manual), providerPaymentId?, providerOrderId?
//
// import { Schema, model, models } from "mongoose";
// const PaymentSchema = new Schema({ ... }, { timestamps: true });
// export const PaymentModel = models.Payment || model("Payment", PaymentSchema);

export {};
