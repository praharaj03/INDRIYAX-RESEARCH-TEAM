// TODO (Backend Dev): Implement Event Mongoose model
//
// Fields: slug, title, type (past|upcoming), thumbnail, description,
//         speaker, date, venue, summary?, recordingLink?
//
// import { Schema, model, models } from "mongoose";
// const EventSchema = new Schema({ ... }, { timestamps: true });
// export const EventModel = models.Event || model("Event", EventSchema);

export {};
