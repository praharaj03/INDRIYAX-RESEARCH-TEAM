// TODO (Backend Dev): Implement Registration Mongoose model
//
// Fields: clerkUserId, name, email, phone?, eventId, eventTitle,
//         eventDate, status (pending|confirmed|cancelled), paymentId?
//
// import { Schema, model, models } from "mongoose";
// const RegistrationSchema = new Schema({ ... }, { timestamps: true });
// export const RegistrationModel = models.Registration || model("Registration", RegistrationSchema);

export {};
