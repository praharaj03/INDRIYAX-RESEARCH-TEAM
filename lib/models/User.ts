// TODO (Backend Dev): Implement User Mongoose model (synced from Clerk via webhook)
//
// Fields: clerkUserId (unique), name, email (unique), imageUrl?, phone?, role (user|admin)
//
// import { Schema, model, models } from "mongoose";
// const UserSchema = new Schema({ ... }, { timestamps: true });
// export const UserModel = models.User || model("User", UserSchema);

export {};
